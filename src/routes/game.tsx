// Placeholder React component for mounting the Chronicles of Eden game route.
export function GameRoute() {
  return (
    <main>
      <button type="button">Back to Home</button>
      <div id="game-container">{/* Phaser game will mount here in future prompts. */}</div>
    </main>
  );
}

export default GameRoute;
